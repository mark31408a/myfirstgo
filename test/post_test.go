package test

import (
	"testing"
	// "fmt"
	// "example.com/myfirstgo/entity/post"
)

func TestPost(t *testing.T) {
	//NewStub()
    // want := "Concurrency is not parallelism."
    // if got := post.GetPost(); got != want {
    //     t.Errorf("Proverb() = %q, want %q", got, want)
    // }
}